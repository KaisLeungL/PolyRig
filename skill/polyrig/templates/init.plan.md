<!-- polyrig: Template for the target project's init.plan.md — the human-reviewable initialization plan. Anything non-trivial the user must do goes here as a manual follow-up. Remove all `polyrig:` comments from the final artifact. -->

# {{PROJECT_NAME}} — Initialization Plan

> Generated by PolyRig {{POLYRIG_VERSION}} on {{GENERATED_AT_DATE}}.
> Review this plan, then run `./init.sh`. The script only creates context files
> and safe directories, and runs `git init` when the target is not already
> inside a repository — everything under "Manual follow-ups" stays manual.

## What will be created

<!-- polyrig: List every file and directory that generation + init.sh will create in the target project. Keep the list exact — this is what the user reviews. -->

- `SPEC.md` — requirements, stack decisions, constraints
- `AGENTS.md` — primary agent instruction file (routing + hard rules)
- `CLAUDE.md` — thin Claude Code entry pointing to AGENTS.md
- `feature_list.json` — plan-as-state feature list
- `docs/stacks/{{STACK_SHORT_NAME}}/` — copied stack pack knowledge
- `docs/domains/{{DOMAIN_SHORT_NAME}}/` — copied domain pack knowledge
- `docs/verify.md` — merged verification routes
- `deps.resolved.md` — dated dependency resolution snapshot
- `.polyrig/manifest.json` — provenance and audit chain
- `.env.example` — empty placeholder for environment variables
- git repository (`git init`) — only when the target is not already inside one
- `.gitignore` — minimal ignore list (`.env`); created only if absent, extend per stack
- {{ADDITIONAL_CREATED_ITEM}}

## What will NOT be done automatically

<!-- polyrig: Everything below is deliberately manual. Expand each checkbox with project-specific detail (which package manager, which build files, which credentials) but never move these into init.sh. -->

Manual follow-ups for you:

- [ ] Install dependencies: {{DEPENDENCY_INSTALL_FOLLOW_UP}}
      (see `deps.resolved.md` for verified versions — init.sh never installs packages)
- [ ] Edit build files: {{BUILD_FILE_FOLLOW_UP}}
      (init.sh never edits build files)
- [ ] Set up credentials: {{CREDENTIAL_FOLLOW_UP}}
      (copy `.env.example` to `.env` and fill it in; `.env` must stay out of version control)
- [ ] Make the first commit after reviewing the generated context:
      `git add -A && git commit -m "chore: polyrig init"`
      (the context layer must enter git so it travels with the repo; init.sh never commits)
- [ ] {{ADDITIONAL_MANUAL_FOLLOW_UP}}

## Environment prerequisites to verify

<!-- polyrig: One bullet per tool/runtime the selected stacks require, with the check command. Source these from the selected stack packs' verify routes. -->

- {{PREREQUISITE_TOOL}} — check with: `{{PREREQUISITE_CHECK_COMMAND}}`

## Next step

1. Review this plan.
2. Run `./init.sh`.
3. Work through the manual follow-ups above.
4. Read `AGENTS.md` and start with the first feature in `feature_list.json`.
