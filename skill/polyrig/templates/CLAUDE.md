<!-- polyrig: Template for the target project's CLAUDE.md — a thin Claude Code entry point. Do NOT duplicate anything from AGENTS.md here. Remove this comment from the final artifact. -->

# {{PROJECT_NAME}}

This project was initialized by PolyRig.

The primary agent instructions live in **`AGENTS.md`**.

Read `AGENTS.md` before doing any feature work; it routes to the spec, the
feature plan, the copied pack knowledge, verification routes, and dependency
resolution records.
