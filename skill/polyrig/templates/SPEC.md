<!-- polyrig: Template for the target project's SPEC.md. Fill every {{PLACEHOLDER}} from interview answers (phases 1-6). Remove all `polyrig:` comments from the final artifact. Artifact language: English. -->

# {{PROJECT_NAME}} — Specification

> Generated by PolyRig {{POLYRIG_VERSION}} on {{GENERATED_AT_DATE}}.
> Implementation plan and state live in `feature_list.json`.
> Agent instructions live in `AGENTS.md`.

## 1. Project identity

- **Name:** {{PROJECT_NAME}}
- **Purpose:** {{PROJECT_ONE_LINE_PURPOSE}}
- **Repository layout:** {{REPO_LAYOUT_DECISION}}
  <!-- polyrig: e.g. monorepo vs. separate repos — record the user's choice and, in one sentence, why. -->

## 2. Requirements summary

<!-- polyrig: Summarize the user's requirements from the interview in short, checkable statements. One bullet per requirement. Detailed acceptance criteria belong in feature_list.json, not here. -->

- {{REQUIREMENT_SUMMARY_ITEM}}

## 3. Stack decisions

<!-- polyrig: One subsection per selected stack pack. Repeat the block below for each stack. Rationale must state WHY this stack was chosen over alternatives, in the user's own terms from the interview. -->

### {{STACK_PACK_ID}}

- **Decision:** {{STACK_DECISION}}
- **Rationale:** {{STACK_RATIONALE}}
- **Knowledge location:** `docs/stacks/{{STACK_SHORT_NAME}}/`

## 4. Selected domain packs

<!-- polyrig: One row per selected domain pack, including packs pulled in via `requires`. The "Why" column records the user's reason for selecting it (or "required by <pack>" for transitive selections). -->

| Pack | Why selected | Knowledge location |
|---|---|---|
| {{DOMAIN_PACK_ID}} | {{DOMAIN_PACK_WHY}} | `docs/domains/{{DOMAIN_SHORT_NAME}}/` |

## 5. Architecture constraints

<!-- polyrig: Hard architectural rules from interview phase 4 (constraints): platform rules, offline/online policy, layering rules, performance budgets. One bullet each; keep them enforceable, not aspirational. -->

- {{ARCHITECTURE_CONSTRAINT}}

## 6. Domain boundaries

<!-- polyrig: State what each module/domain owns and what it must not reach into. Keep it to boundaries only; implementation guidance lives in the copied pack knowledge under docs/. -->

- {{DOMAIN_BOUNDARY_STATEMENT}}

## 7. Security red lines

<!-- polyrig: Non-negotiable security constraints from interview phase 4, merged with the red lines of the selected packs' pitfalls. Violating any item here blocks a feature from being marked verified. -->

- {{SECURITY_RED_LINE}}
- Never commit secrets, credentials, or tokens to version control. Use `.env` files (git-ignored) with `.env.example` as the committed reference.

## 8. Out of scope

<!-- polyrig: Explicit non-goals stated by the user. Listing them prevents scope creep in later agent sessions. -->

- {{OUT_OF_SCOPE_ITEM}}
