{
  "name": "polyrig",
  "version": "0.1.0",
  "description": "Universal AI harness pack protocol and context assembly system. `npx polyrig install` stages the runtime and installs the /polyrig, /polyrig-pack-author, and /polyrig-pack-install skills for your local agent platforms (Claude Code, Codex, Cursor, Gemini CLI, OpenCode).",
  "license": "MIT",
  "homepage": "https://github.com/KaisLeungL/PolyRig",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KaisLeungL/PolyRig.git"
  },
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "polyrig": "scripts/link-skill.mjs"
  },
  "files": [
    "scripts/",
    "!scripts/test-*.mjs",
    "!scripts/fixtures/",
    "packs/",
    "schemas/",
    "skill/",
    "docs/",
    "!docs/plans/",
    "SPEC.md",
    "README.md",
    "README.zh-CN.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "node scripts/test-link-skill.mjs && node scripts/test-build-pack-index.mjs && node scripts/test-validate-pack-evidence.mjs && node scripts/test-validate-pack-verify-tools.mjs && node scripts/test-validate-artifacts.mjs && node scripts/test-install-pack.mjs",
    "doctor": "node scripts/doctor.mjs"
  }
}
